# The public repo holds ONLY what the live site needs: index.html, assets, README,
# and the worker README. Everything else is authoring, testing or local scratch.
#
# NOTE (2026-08-24): this file is edited ON THE DEVICE. A session that edits its own
# container copy will be working from a stale version and will silently drop lines.
# Stage this file first, or edit it here.

# Local-only dev and authoring files
CLAUDE.md
SPEC.md
build_bank.py
splice.py
template.html
collide.py
verify.py
serve.ps1

# Test suites. Pattern rather than a list of names, because naming them one by
# one meant storetest.py got committed the moment it was written (2026-08-25).
# worker/test.mjs is .mjs and stays tracked: it is part of the worker.
*test*.py
smoke.py
# and the standalone measurement tools the gates are built on (SPEC 27)
poscheck.py

# One-off patch scripts. Pattern, not a list: naming them one by one meant
# fix8.py was committed the moment it was written (2026-08-25), which is the
# same mistake the test-file list made a day earlier.
fix*.py

# Generated intermediates and reports
dev/
content/
docs/
shot-*.png
blind-*.json
collision-report.md
sentences.jsonl
frames.jsonl
families.jsonl
cloze.jsonl
classcloze.jsonl
transform.jsonl
policy.json
ambig.json
__pycache__/
*.pyc

# Cloudflare worker: local state and secrets. The repo is public (SPEC 19).
worker/.wrangler/
worker/.dev.vars
worker/node_modules/
.dev.vars

# Local helpers and scratch
deploy.bat
deploy-worker.bat
_gitscratch/

# OS / editor cruft
Thumbs.db
ehthumbs.db
.DS_Store
Desktop.ini
*.tmp
*~
node_modules/

# Quiz authoring (the Forms import doc is a deliverable, not part of the site)
quiz/

# shared test navigation helper (dev only, not needed by the live site)
wccnav.py
technames.json

# Analysis Acrobat import sources (v0.34): authoring, not site
build_aa.py
aa_bank.py
imagery_bank.py

# Analytical language sources (v0.41): authoring, not site
build_anal.py
anal_bank.py

# Techniques (Expanded) second-wave sources (v0.42): authoring, not site
build_techx.py
techx_bank.py

# Teaching resources kept beside the app, never published
*.pptx
*.pdf

# v0.44: build logos, test runner, reviewer brief, and every generated bank file
build-assets/
run-tests.sh
AUDIT-BRIEF.md
*.jsonl
/*.json

# v0.46: frozen id maps (authoring; never regenerate) 
transform_ids.py
xterm_ids.py

# v0.50: anything of the students' own, kept beside the app and never published
Student Surveys/
yeartest.py

# Diagnostic screener sources (v0.61): authoring, not site. The register and the
# assembler are build-time; the abandoned parallel bank is kept only as raw
# material for step 6 (SPEC 72.1).
build_dx.py
dx_holdout.py
dx_bank.abandoned.txt
wcc-v*.tgz

# Screener blind-gate material (v0.61): rendered from the app, read by reviewers
dxrender.py
dx-review-*.md
